## Codebase Patterns
- **DB Migrations**: `npm run db-migrate` won't work without local Docker — update `supabase/types.ts` manually, then apply SQL via Supabase Management API. Use `IF NOT EXISTS` for safety.
- **types.ts format**: Tables sorted alphabetically, each entry has Row/Insert/Update/Relationships; fields within each block also sorted alphabetically
- **Supabase project ref**: `mlfpqlxqwdifklwunnqe` (extracted from NEXT_PUBLIC_SUPABASE_URL)
- **Typecheck**: `node_modules/.bin/tsc --noEmit` — pre-existing warnings in lint are not caused by our changes
- **UI Select**: Radix UI Select is at `@/components/ui/select` — import `Select, SelectTrigger, SelectValue, SelectContent, SelectItem`
- **Model forms**: create-model.tsx and model-item.tsx both use the pattern: state hooks → createState/updateState → renderInputs; new DB fields can be added via the `as TablesInsert/TablesUpdate` cast
---

# Ralph Progress Log
Started: 2026-03-27

## 2026-03-27 - US-001
- Created migration file `supabase/migrations/20260327100000_add_provider_type_to_models.sql` with:
  - `ALTER TABLE models ADD COLUMN provider_type TEXT NOT NULL DEFAULT 'openai'`
  - `ALTER TABLE models ADD COLUMN thinking_budget_tokens INTEGER NOT NULL DEFAULT 0`
- Manually updated `supabase/types.ts` to add `provider_type: string` and `thinking_budget_tokens: number` to models Row, Insert, and Update types
- Applied migration to cloud DB via Supabase Management API (verified columns exist)
- Typecheck passes
- Files changed: `supabase/migrations/20260327100000_add_provider_type_to_models.sql` (new), `supabase/types.ts`
- **Learnings for future iterations:**
  - Cloud DB API returns `[]` on success for DDL statements
  - Use `IF NOT EXISTS` in the API call even if migration file doesn't have it, for idempotency
  - New fields with defaults don't break existing code — typecheck passes without any consumer changes
---

## 2026-03-27 - US-002
- Added `customProviderType?: "openai" | "anthropic"` to LLM interface in `types/llms.ts`
- Updated `components/models/model-select.tsx` to pass `customProviderType: model.provider_type as "openai" | "anthropic"` when mapping custom models to LLM objects
- Typecheck passes
- Files changed: `types/llms.ts`, `components/models/model-select.tsx`
- **Learnings for future iterations:**
  - The `LLM` interface is the central type used across the app for model selection and routing — adding fields here makes them available everywhere
  - Custom models are mapped to LLM objects in `model-select.tsx` `allModels` array — this is where DB fields get exposed to the UI/routing layer
---

## 2026-03-27 - US-003
- Added "Provider Format" Select dropdown (OpenAI Compatible / Anthropic Native) to both create-model.tsx and model-item.tsx
- Added "Thinking Budget (tokens)" number input that appears only when Anthropic is selected
- Base URL placeholder changes to `https://api.anthropic.com` when Anthropic is selected
- Help text dynamically switches between OpenAI SDK and Anthropic SDK compatibility messages
- Both `provider_type` and `thinking_budget_tokens` are passed in createState/updateState
- model-item.tsx reads saved values from `model.provider_type` and `model.thinking_budget_tokens`
- Typecheck passes
- Files changed: `components/sidebar/items/models/create-model.tsx`, `components/sidebar/items/models/model-item.tsx`
- **Learnings for future iterations:**
  - Radix UI Select component is available at `@/components/ui/select` — use `Select`, `SelectTrigger`, `SelectValue`, `SelectContent`, `SelectItem`
  - Both create and edit model forms follow the same pattern: state hooks → createState/updateState object → renderInputs JSX
  - The `as TablesInsert<"models">` / `as TablesUpdate<"models">` casts mean you can pass new DB fields directly without changing the sidebar framework
---
